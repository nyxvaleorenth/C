#include <math.h>

int main()
{
    float x = 0.0f;
    int y = 0;

    x = sqrt(x);  // square root
    x = pow(x, 2);  // pow(base, power)
    x = round(x);
    x = ceil(x);
    x = floor(x);
    // x = abs(x); something is wrong
    x = log(x);
    x = sin(x);
    x = cos(x);
    x = tan(x);

    return 0;
}
